# Screenshots

Add screenshots of your running app here:
- `home.png`  — homepage view
- `prediction.png` — after a digit has been predicted

You can take these with any screenshot tool once you run `streamlit run app.py`.
